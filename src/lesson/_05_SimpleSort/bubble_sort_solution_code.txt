//Funkcje specjalne:
void start(&n){}
//-----------------------
int porownaj(idx1,idx2){}
void zamien(idx1,idx2){}

//Porządkowanie bąbelkowe
//Rozwiązanie wzorcowe:
void uporzadkuj(n)
var i, j, idxMin;
{
//todo
  for(i=0; i<n-1; i++) {
    idxMin = i;
    for (j=i; j<n; j++) {
      if (porownaj(j,idxMin)<0) {
         idxMin = j;
      }
    }
    zamien(i, idxMin);
  }
}

void main()
var n;
{
  start(n);
  uporzadkuj(n);
}